/**
 * Global constants for touch and pointer interactions.
 */

/**
 * Duration in milliseconds a user must hold down a touch pointer before it is recognized as a long-press.
 */
export const LONG_PRESS_DURATION = 500;

/**
 * Distance in pixels a user's touch pointer can move before cancelling a long-press.
 */
export const LONG_PRESS_MOVE_THRESHOLD = 10;
